# Base domain for the Caddy reverse proxy (the app is served on shopping.$DOMAIN)
DOMAIN=example.com

# Secret for signing JWT tokens. Required in production — generate one with:
#   openssl rand -hex 32
JWT_SECRET=
